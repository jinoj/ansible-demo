---
# tasks file for ansible/jino
- name: 'install nginx packages'
  package:
    name: nginx
    state: present
- name: 'start nginx service'
  service:
    name: nginx
    state: started
    enabled: yes
